import React from 'react'
import './Footer.css'
import PlusButton from './PlusButton.png'

export default () => {
  return(
    <footer>
      <img src={PlusButton} alt="PlusButton" />
    </footer>
  )
}