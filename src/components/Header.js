import React from 'react'
import './Header.css'
import { Link } from 'react-router-dom'

export default () => {
  return(
    <nav id="topNav">
      {/* <img src={Note} alt="note-logo" /> */}
      <h3 id="headerTitle">simple note</h3>
    </nav>
  )
}